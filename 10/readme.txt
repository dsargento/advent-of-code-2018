Explanation:

I had to iterate a lot for the values to get near the intersection point
Then we manually increment using user input until we get the output we want (result.png)
The result is upside down, flip it in a image manipulation program, and we can read the answer is zzcbggcj

Explanation part 2:

Just add a counter and print the number of iterations to get the answer